
let NetworkOptional = {}

let NetworkRequired =
  { security_group_ids : (List Text)
  , subnet_ids : (List Text)
  , vpc_id : Text
  }
let Network = NetworkOptional //\\ NetworkRequired

in
{ Network = Network
}
